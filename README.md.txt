this app was built with webPack and node package manager...
firstly you would need to run "npm install" to install the required packages for this program to run
secondly, 
to run the application open terminal and run "npm start"